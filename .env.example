# ============================================================
# Peopole AI — Earth Solutions Visa Zone
# Environment Variable Template
# ============================================================
#
# NEVER commit real secrets.
# Local development:
#   copy .env.example to .env.local
#
# Vercel:
#   configure variables in Project Settings → Environment Variables
#
# ============================================================

# ------------------------------------------------------------
# Application
# ------------------------------------------------------------

NODE_ENV=development
PORT=3000

# ------------------------------------------------------------
# AI Provider
# ------------------------------------------------------------

GROQ_API_KEY=REPLACE_WITH_GROQ_API_KEY

# ------------------------------------------------------------
# CORS / Origin Protection
# ------------------------------------------------------------

# Local development
ALLOWED_ORIGIN=http://localhost:3000

# ------------------------------------------------------------
# Admin Authentication
# ------------------------------------------------------------

# Use a long, random secret in staging/production.
ADMIN_PASSWORD=REPLACE_WITH_LONG_RANDOM_ADMIN_SECRET

# ------------------------------------------------------------
# Web Push / VAPID
# ------------------------------------------------------------

VAPID_PUBLIC=REPLACE_WITH_VAPID_PUBLIC_KEY
VAPID_PRIVATE=REPLACE_WITH_VAPID_PRIVATE_KEY
VAPID_EMAIL=mailto:admin@earthsolutions.com.bd

# ------------------------------------------------------------
# Database
# ------------------------------------------------------------

# Used by node-pg-migrate / future durable application storage.
DATABASE_URL=REPLACE_WITH_NEON_DATABASE_URL

# ------------------------------------------------------------
# Optional application environment metadata
# ------------------------------------------------------------

APP_ENV=development
